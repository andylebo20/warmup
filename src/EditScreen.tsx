import React from "react";

type Props = {
  goBack: () => void;
};

export const EditScreen = ({ goBack }: Props) => (
  <div
    style={{
      display: "flex",
      flexDirection: "column",
      justifyContent: "flex-start",
      alignItems: "center",
      paddingTop: 20,
    }}
  >
   Cade McNamara

  </div>
);
